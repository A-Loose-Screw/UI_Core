apply from: project.file("$projectDir/../shared/ext.gradle")

ext {
  nativeName = "UI-Core"
  binarySpec = NativeLibrarySpec
  req_proj_libs = ['GLFW', 'GLAD', 'IMGUI']
  linkage = 'static'
}

apply from: project.file("$projectDir/../shared/cppBuild.gradle")
